#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int decimal;

	for (decimal = 0; decimal < 10; decimal++)
	{
		printf("%d", decimal);
	}
	printf("\n");

	return (0);
}
