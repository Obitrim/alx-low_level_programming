#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int decimal;

	for (decimal = 48; decimal < 58; decimal++)
	{
		putchar(decimal);
	}
	putchar('\n');

	return (0);
}
